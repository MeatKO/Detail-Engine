#pragma once

#include <iostream>
#include <string>
#include <sstream>

#include "ECS.hpp"
#include "dMath.hpp"

namespace detailEngine
{
	struct Texture
	{
		// things
		// will not hold actual pixel data, only id of the texture and potentially a name ?
		// turn this into asset ?
	};

	struct Material
	{
		// more things
	};

	struct Mesh
	{
		// even more
	};

	// getting over my head on this one
}